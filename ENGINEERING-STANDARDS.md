# PXD / LS Studio 开发与验收规范

更新：2026-09-21。适用于本仓库的开发者、实现 agent、reviewer 和集成人。本文是工程规范的主来源；[AGENTS.md](AGENTS.md) 负责开工入口，[PR 模板](.github/pull_request_template.md) 负责交付记录。个人 Skill 可以引用本文，不另存一份逐渐失同步的规范。

## 1. 开工前：说明改动会影响什么

先读本文，再读目标模块的现行契约。规划文档、代码已存在、自动化通过、真机通过、允许合并是不同事实，不得互相替代。

每项改动先确定四件事，写入任务记录，提交 PR 时总结：

1. 应用和宿主：根目录 `src/` 是原 PXD React/TypeScript 应用；`apps/ls-studio/` 是 LS Studio 的 HTML/JavaScript UXP 面板和 Node Companion。两个应用都可能有 Photoshop 界面，不能仅凭 React 判为浏览器专属。明确本次影响浏览器预览、PS 面板、Companion、HTTP/MCP 中哪些部分。
2. 用户路径：用户从哪里进入、编辑什么、触发什么副作用、成功和失败后看到什么。配置载入不等于生成、生成成功不等于回贴成功。
3. 影响清单：实际使用到的控件类型、布局容器、主题／缩放、能力参数、状态、公共错误和存储格式。新增模块也要检查它是否使用已有公共样式及适配器。
4. 所有权和验证：实现者负责的文件、共享入口集成人、独立 reviewer、适用的自动化和真机场景。先确定接口再并行实现，避免最后才发现协议或组件不一致。

这是工程记录要求，不要求每次例行实现再向用户索取批准。已有授权和协作边界继续有效；需要改变产品范围或不可逆操作时按原规则处理。

## 2. 前端：沿用设计系统，完整适配控件

### 2.1 视觉与文案

- 复用目标应用现有的颜色、字号、行高、间距、边框和交互组件。LS Studio 的依据是 [styles-014.css](apps/ls-studio/plugin/styles-014.css)、[scale-014.js](apps/ls-studio/plugin/scale-014.js)、[ui-014.js](apps/ls-studio/plugin/ui-014.js) 和 [composer-014.js](apps/ls-studio/plugin/composer-014.js)。它们也可能有缺陷，复用不能代替实测。
- 修兼容问题时保持既有设计方向。新组件先与相邻组件对齐，不随意替换字体、圆角、阴影、密度或主题。不要把浏览器字体／渲染行为当作 UXP 的保证。
- 表单标签明确参数含义、单位和默认行为；输入值不能只靠 placeholder 表达。保留空值、`0`、显式清除、继承服务默认之间的差异。
- 明确覆盖常态、焦点、禁用、加载、空状态、错误和成功。禁用需要解释原因；文字仍须可读。颜色之外还要有可理解的文字或状态标识。
- 产品文案表达用户操作与结果。内部 requestId、revision、原始异常和协议细节放在必要的诊断位置，不能代替用户可执行的提示。

### 2.2 UXP 控件检查清单

每次增加或修改表单，按实际控件类型核对下面的规则；不能仅给所有元素挂同一个 class 就算适配完成。

| 控件 | 必须核对 |
|---|---|
| text / search / 未显式写 type 的 input | 字体、背景、边框、实际高度、焦点、中文输入。`[type=text]` 不匹配没有 type 属性的 input；显式设置类型或在公共规则中覆盖默认文本输入 |
| number | 必须覆盖原生外观、系统字体、非透明背景和尺寸规则；检查小数、0、空值、边界和失焦后的显示，后端另做校验 |
| password | 保持密码类型，以虚拟 Key 实测遮罩、编辑及提交后清空；不能通过改成明文解决样式问题 |
| select / option | 关闭时、展开时、选中后及禁用时分别检查；选择能真正更新状态，恢复“模型默认”能移除旧覆盖值；不能从输入框样式推断下拉框已适配 |
| textarea / composer | 长文本可读和可滚动，焦点反馈清楚；Enter、Shift+Enter、输入法候选确认分别验证，符合设置中的发送语义 |
| checkbox / radio / range / file | 保留专门的行为、尺寸和隐藏状态；不得被通用“输入框修复”选择器覆盖成文本框 |
| 按钮或自定义 HTML 控件 | 复用键盘／disabled 适配；鼠标、Enter、Space、焦点移动有效，长按不重复执行；单有 role 或 tabindex 不构成键盘支持 |

UXP 原生控件可能拥有自己的绘制、字体和事件行为。新增 CSS 或事件方案必须在受支持的 PS/UXP 版本核对；必要时查 [Adobe UXP 文档](https://developer.adobe.com/photoshop/uxp/)，记录实际版本与限制。浏览器中有效的 `gap`、透明输入背景、字体、伪类或合成事件不得未经验证直接用于原生验收。优先复用已验证的降级实现。

### 2.3 尺寸、缩放和滚动

- 计算完整盒模型：控件总高度至少容纳文字行高、上下 padding 和边框。比如 16px 行高、上下各 6px padding、各 1px 边框已经需要 30px；28px 会裁切。32px 是这组参数的可用参考值，不是全项目无条件固定高度。
- 尺寸与字体使用同一缩放体系，新增 token 同步到缩放表。不要只放大外框或只依赖 `transform: scale()`；缩放后必须重新核对实际字形、内容尺寸和点击范围。
- 一条纵向内容流明确一个主要滚动容器。长内容允许增高并滚动，不能靠固定高度、负 margin 或隐藏溢出来掩盖重叠。
- flex 纵向页面中的完整区块要保留内容高度。检查**直接子挂载容器**的收缩规则；只给内部区块 `flex: none` 不能防止外层被压缩。负责滚动的 flex 子元素根据需要设置 `min-height: 0`，可收窄的列设置 `min-width: 0`。
- 多字段布局按标签／数值需要的实际宽度折行。不能用每列 25% 或过小的固定最小宽度，挤掉模型名、单位或操作。长中文、URL、模型名和错误提示都要试。
- 在浅／深色、100%／200%、常规／窄面板的组合中检查改动区域；缩放控件本身被修改时，还须试可选的中间档位。记录实际面板尺寸。滚动到底部确认末尾操作可达、无意外横向滚动条／小方块，也不因隐藏滚动条失去可操作性。

## 3. 后端：状态、错误和副作用是公开契约

### 3.1 能力与共享状态

- LS Studio 的 UI HTTP 和 Agent MCP 是同一能力服务的适配层。遵循 [domain 契约](apps/ls-studio/companion/domain/README.md)，不在 UI、Agent、Skill 各实现一次生成、参数编译或 PS 写入。Skill 指导如何使用能力，不能成为另一份业务状态。
- 参数、能力限制和错误码有明确主来源。前端用于引导，后端在边界独立校验类型、范围、长度、模型能力和实际图像尺寸；disabled 和前端校验不是安全边界。
- 草稿可编辑，已提交 Job 的输入、参数、来源、模型和结果快照不可变。显示实际执行模型，不能用当前服务默认值改写旧任务。图片通过受管理的资产 ID 引用，不接受客户端任意磁盘路径。
- UI 与 Agent 使用同一 revision。过期写入显示冲突，保留本地编辑，提供重新载入或派生草稿的明确路径；不能自动覆盖。异步请求绑定相应草稿／连接／请求序列，旧响应不得覆盖新状态。
- 变更公共 schema、错误码、存储或能力限制时，同步消费者、文档和针对性回归；覆盖已有数据与恢复路径。不声称目录或工具名存在就代表功能已可用。

### 3.2 错误从服务到最终页面都必须安全、准确

公共错误由稳定 code 和安全字段组成。每个入口及其调用方都不能显示供应商原文、原始异常、URL 中的凭证或未经清理的 details/cause。表单里换成安全提示后再抛原 Error，仍会在父工作区／Agent 中泄漏，必须检查完整传播链。

| 真实情况 | 界面和恢复行为 |
|---|---|
| 读取失败 | 说明没有读到什么、该检查哪个连接或配置；不得提示“提交未确认”或“重试沿用请求编号” |
| 明确拒绝且未写入 | 显示可操作的原因，如版本冲突、参数错误、损坏配置或缺少能力；不泛化为网络未知状态 |
| 请求结果不确定 | 明确尚未确认，先读回／核对已记录结果；不自动重发付费或 PS 写入请求 |
| 保存已确认，但后续刷新失败 | 分别显示“已保存”和“刷新失败”；不能误导用户再次保存或认为写入失败 |
| 原子 rename 后持久化确认失败 | 可能已经写入；保留结果不确定语义，说明存储问题并要求读回核对 |
| 生成成功，但回贴失败 | 保留生成结果和失败回执；重试回贴不能重新生成 |
| 未知错误码 | 使用安全兜底和诊断标识；不透传未知 code/message，也不伪装成成功 |

设置中的 Key 只在必要的私有配置边界处理，前端读取接口不返回 Key。虚拟 Key 要覆盖表单、父页面、错误事件、日志、持久化任务和 Agent 输出的泄漏检查。提交后及时清空输入；环境变量提供的字段和本地字段分别显示来源、分别限制编辑。不得记录真实凭证做验收。

日志用于定位实际问题：可记录操作名、公开 ID、状态、耗时、版本和安全错误码；避免原始请求／响应、headers、Key、用户提示词、会话或图片内容。诊断输出不等于副作用已成功，必须有实际结果／回执。

### 3.3 副作用与恢复

- 先持久化请求标识和输入快照，再调供应商或写 PS。运行、回贴、回滚分别去重；重连、轮询、重复点击或恢复进程不自动重做副作用。
- 取消请求、停止 Agent 对话、撤销 PS 修改是不同操作。取消后迟到结果不能把任务恢复成成功或自动写 PS；支持保留结果时仍归属原任务。
- 所有本机 HTTP 路由，包括旧路由，按同一 Host／Origin／客户端标识／方法／体积边界处理。监听 loopback 不代表浏览器无法跨站触发请求。对桥接 token、配置写入和供应商调用分别检查授权，不扩大网络或文件权限来绕过问题。
- PS 抓图与写入绑定文档的当前运行期／打开实例及预期状态；文档切换、关闭重开、后续编辑不能错抓、错贴或粗暴回滚。宿主写入由集成人串行安排。
- 真实选区包含孔洞、羽化和 alpha；矩形范围不能冒充 mask。记录源／输出尺寸与坐标变换，在实际图像上观察边界、回贴与撤销。图层修改检查错误描述符和实际后态，不能仅以“Promise resolve／没有 throw”认定成功。

## 4. 实现、独立 review、组装与验收

| 阶段 | 负责人必须完成 | 不能替代的后续步骤 |
|---|---|---|
| 实现 | 影响清单、契约／公共组件复用、针对性正常与失败路径测试 | 独立 review 和真实渲染 |
| 独立 review | 不同 reviewer 检查最终 diff、调用方、模块边界；提出可复现问题并复查修复 | GitHub 的独立审批及真机操作 |
| 组装 | 集成人接入共享入口，检查相邻模块／动态字段、UI 与 Agent 接续；记录具体源码组合 | 单模块测试不能当作整套产品通过 |
| 目标宿主验收 | 在所有受影响的实际宿主操作最终候选提交；涉及 PS/UXP UI 必须在原生面板操作，浏览器专属 UI 在实际浏览器操作；检查前端与后端结果，记录版本、动作和观察 | 浏览器 UI 调用 PS 后端，只证明后端路径 |
| 合并／发布 | 核对最终 head 的检查、审批、讨论和适用证据，沿用现有 main／release 门禁 | 规范、模板、fixture 或截图不会自动授予合并资格 |

验证分为**自动化、浏览器交互、PS 后端执行、PS 原生面板视觉／输入**四类，逐项记录通过／失败／未验证及证据。不要把测试数量相加、把 mock 生成说成真实供应商、把截图或初始化日志说成已操作过界面。既要检查操作后的后端状态，也要看用户当前看到的前端状态。

对改动的实际页面至少做一条完整操作路径，覆盖主成功路径和一个有意义的失败／恢复路径。涉及 PS/UXP 的 UI 变更还必须实际执行第 2 节适用原生控件、主题和尺寸组合；浏览器专属 UI 在实际浏览器验证受影响的控件、状态和布局，并说明为何原生面板检查不适用。后端变更按其契约核对重试、冲突和副作用。测试范围按风险选择，避免为了数量重复同一断言；文档改动做链接和规则一致性检查，不伪造运行结果。本段定义检查范围，不改变既有 main／release 门禁。

以下问题阻塞受影响的验收：重叠／裁字／不可达按钮、黑条或不可读输入、焦点或键盘不符合约定、控件值与提交状态不一致、错误提示错报结果、敏感内容外泄、重复副作用、错文档写入、依赖集成后退化。不得以“只是 UI”“CI 已绿”跳过；本轮截图反馈应转化为控件清单、回归用例或明确的真机案例。

遇到无法控制窗口、UXP 加载超时、缺少所需供应商／样片时，记录已经执行的步骤、实际错误和未验证项，保持相关验收阻塞。不能为了验收重启带有未保护用户数据的 PS、替换 Alpha 服务、扩大权限或制造成功状态。能独立完成的代码和文档工作继续进行。

## 5. PR 必须留下可复核的交付记录

使用 [PR 验收记录模板](docs/v2/templates/PR-ACCEPTANCE.md)。每个 PR 说明用户可见结果、依赖／比较 base、影响清单、验证范围、独立 reviewer 和仍未解决的问题。Draft 可以在开发中创建；基本交互失败或证据缺失时不能宣布验收完成。

记录验证的 source commit、插件来源／构建对应关系、宿主版本、专用测试素材、实际操作、观察结果与证据位置。新提交不得沿用旧提交的成功结论；组装分支的结果不能直接移给源码不同的模块 PR。验收报告可以在后续独立 evidence commit 中引用被测试的 source commit，避免自指 SHA。

每一项用“通过／失败／未验证／不适用＋原因”填写；不适用是范围说明，不是成功证据或门禁豁免。文档 PR 不得自动把 `ls-studio/computer-use` 标绿；人工审批、现有保护和 release 证据契约仍按 [协作规则](docs/v2/COLLABORATION.md) 与 [CI/CD](docs/v2/CI-CD.md) 执行。浏览器、后端或前端的任意一项通过，都不能替另一项通过。

本规范不回填旧验收，不代表现存 UI 已修复。2026-09-21 暴露的布局／控件问题及当前修复范围见 [PR #66](https://github.com/lianshuang-photo/PXD/pull/66)；合并状态、最终 head 与真机结果以对应 PR 的最新记录为准。
