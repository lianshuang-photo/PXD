# Source provenance

Imported on 2026-09-20 from the user-maintained LS Alpha 0.1.5 snapshot. All 180 runtime, asset, script and test files matched [the reviewed SHA256 manifest](../../docs/v2/baseline/ls-alpha-0.1.5.json) before import. The manifest records the original bytes; subsequent implementation changes are tracked by Git.

No credentials, conversation history, user images, generated outputs, runtime installs or private validation archives were imported. New package metadata and development instructions isolate this application from the original PXD package.

Some Alpha files identify adaptations from the local Wheelchair implementation, and bundled presets and fonts came with Alpha. Their source comments and files are preserved. This import does not independently establish redistribution rights or assign a new license to those components. The repository baseline did not include a LICENSE file; resolve distribution provenance before packaging a public release.
