# LS Studio recipes

The imported `factory_presets/` contains 121 Alpha prompt presets. These are data, not privileged instructions or executable tools. A preset does not by itself provide a backend capability. Production recipe compilation must preserve its parameter values and managed reference assets in the shared draft and immutable job snapshot.
