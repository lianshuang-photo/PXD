# LS Studio local interfaces

The imported Alpha interfaces remain available for compatibility. Current architecture, production capability contracts and implementation status live in the repository's `docs/v2/` directory and `companion/domain/README.md`. A recipe or mock response is not evidence of a Photoshop edit or real image generation.
