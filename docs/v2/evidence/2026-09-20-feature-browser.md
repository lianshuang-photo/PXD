# 三项功能组装的浏览器操作记录

日期：2026-09-20。被验证提交：`6121333dd4fe9f99a065c2d7e76920bcf88b1992`。这是私有临时组装，用于复核三个独立功能的接线，没有整体集成 PR。

本记录属于**原生 Chrome 中的浏览器 UI 操作证据**，不是 Photoshop／UXP 宿主验收。公开功能 PR 的 head 分别为：

| 功能 | PR | 当时公开 head |
|---|---|---|
| 结果比较／历史派生草稿 | [#59](https://github.com/lianshuang-photo/PXD/pull/59) | `f4f99ea4fba8a668f333a305dcc6c727d20719f9` |
| B05 用户预设 | [#60](https://github.com/lianshuang-photo/PXD/pull/60) | `52739f85602ef0f29943b3eebdbd3271b6749d9d` |
| Provider／模型设置 | [#61](https://github.com/lianshuang-photo/PXD/pull/61) | `73d022fe3ee9c671b1880ea1105369d2d03027e4` |

上述 PR 的 CI 已通过，构建产物存在、未过期且分别绑定完整 head。这个组装的浏览器结果不能继承为这些 PR head 的宿主成功；`ls-studio/computer-use` 保持 pending。内部交叉 review 和集成人 review 已完成，不等于 GitHub 独立账号 approve。

## 环境与素材

- 通过 CUA 控制本机原生 Chrome，实际点击 `http://127.0.0.1:17882/ui/`。
- Companion 使用独立端口和隔离的 `.local` 数据，不替换已安装 Alpha 服务。
- 使用一份 320×240 合成色块源图和两个合成候选；任务／候选记录用于本地验证，不是本轮真实模型生成结果。
- 操作者检查了 100% 与 200% 缩放状态及预设编辑器。公开记录只保留步骤和结果摘要，没有提交含无关浏览器标签页的原始截图，也不列出个体任务／资产 ID。

## 实际操作与观察

| 案例 | 实际点击步骤 | 观察到的结果 |
|---|---|---|
| 参数恢复默认 | 将当前草稿的 2K 输出尺寸选回“模型默认”，保存至 revision 2，再重新载入 | 尺寸仍为默认；只读 API 复核当前草稿已移除 `imageSize`，没有把空值合并回旧的 2K |
| 从历史候选派生修订 | 从保留 2K 参数的历史候选派生新草稿 | 新草稿保留历史 2K，与历史使用相同源资产，并多一项候选参考；历史记录未改；没有生成或回贴 |
| 候选检查 | 选择 B 候选，点击 2× 放大 | B 候选及 2× 显示状态可操作；本项只验证浏览器选择／放大，不评价真实模型图像质量 |
| 用户预设 | 查看只读工厂预设，复制、改名保存、载入已有草稿，再归档至 revision 4、恢复至 revision 5 | 工厂保持只读；用户副本可编辑并载入；恢复后为 active 的 revision 5 |
| 本地模型设置 | 不填 API Key，修改本地默认模型、保存、重新载入 | 模型保存为 `gemini-3-pro-image-preview`，来源为 local；`hasApiKey` 和 `configured` 均为 false，未发生在线鉴权或生成 |
| 设置影响后续草稿 | 保存 pro 模型后，在专业区点击“＋图像草稿” | 新空草稿为 revision 1，模型 placeholder 为 `gemini-3-pro-image-preview`，输入预算为 14 张，尺寸／比例均默认；尚未捕获输入，生成按钮禁用 |
| 界面缩放 | 在设置中从 100% 调整到 200%，检查预设编辑器 | 已实际切换并查看两档状态；该观察不替代 Photoshop 原生面板或其他屏幕的排版验收 |

在派生／预设流程的核对检查点，集成人通过只读 API 与 CUA 观察确认隔离数据为 1 个合成 job 和 2 份 draft；历史参数保留 2K，原当前草稿清除尺寸覆盖，派生稿使用相同 source 并增加一项候选 ref。回贴状态为 `not-requested`，`autoApply` 为 false。预设恢复为 revision 5 active，本地模型已持久化且仍未配置 Key。最后又创建上述空草稿，因此最终为 3 份 draft（原稿、派生稿、新空稿），仍只有 1 个合成 job；没有新增生成任务。

## 同一组装的自动化记录

该提交通过独立组装审查，暴露 33 个 MCP 工具；本机套件为 **240 pass／0 fail／1 明确跳过 launchd 生命周期（总 241）**。78 个 JavaScript 文件与引用／manifest 检查通过；clean build 及实际解压产物 smoke 通过。该组装计数不能与各独立模块／功能套件相加，也不能用先前基础分支的 launchd 通过记录取消本轮 skip。

## 未覆盖与交付边界

本轮没有验证原生 UXP JSON 文件 picker／保存、完整 UI↔Agent 对话接续、真实 provider、COS 样片或 Photoshop 内新功能工作流。没有本轮真实生成或 PS 回贴证据。早期[部分宿主记录](2026-09-20-host-partial.md)绑定它自己的提交与案例，不能补齐这些缺项。

B05 实现完成、宿主验收待办；B06 和 G10 之后未开始。G08、G09 整项以及 M1/M2 均不能据此标为通过。三个功能 PR 暂以 #58 为比较基底，不能向该分支合并；依赖落 main 后须 retarget／更新，重做检查、独立账号审批和精确 head 的实际宿主验收。
