# Implementation status

Updated 2026-09-21. The user now authorizes merging eligible PRs; preservation of Alpha, protected main, independent review and computer-use acceptance remain required. The [current merge-readiness record](evidence/2026-09-21-merge-readiness.md) describes corrections absorbed into module candidates, the new frontend assembly, remaining GitHub/host blockers and the next bounded development task. No main merge has occurred.

Planning #50 and broad integration #51 are closed as superseded without merging. [Module PRs #52–#58](MODULAR-DELIVERY.md) passed CI at the earlier heads recorded in the delivery index. Three implemented features are separate draft PRs: [results #59](https://github.com/lianshuang-photo/PXD/pull/59), [B05 presets #60](https://github.com/lianshuang-photo/PXD/pull/60) and [provider settings #61](https://github.com/lianshuang-photo/PXD/pull/61). Their recorded earlier CI successes do not describe the new #52/#54/#57/#59 heads in the current merge-readiness record; hosted checks must rerun on each new SHA. The features' temporary comparison base is #58, not a merge destination. After prerequisites land on main, retarget/update and renew checks, review and host evidence.

Internal cross-review and integrator review are complete for the three features and their private assembly. Independent GitHub-account approval and exact proposed-commit Photoshop/UXP acceptance are still required. All `ls-studio/computer-use` statuses remain pending. The code in this #58 tree contains the foundation professional UI; the three feature implementations and their own documents are in their linked PRs.

The first [independent acceptance session](evidence/2026-09-20-independent-review.md) did **not pass** the merge/host gate. It independently reran all ten public heads and downloaded/tested all ten CI artifacts, then found an HTTP-origin bypass and late-result model inheritance defect. Real capture and a combined layer edit succeeded, but rollback coincided with a native Photoshop crash and an uncertain outcome. Corrections and their separate retests are tracked in the [delivery index](MODULAR-DELIVERY.md); the original acceptance commit remains unchanged.

A [second independent host round](evidence/2026-09-20-independent-review-round2.md) on `8ab0e1af33510600b26215e34c0c5c272088635a` passed fresh native edit/rollback, later-user-edit refusal, cross-document and close/reopen refusal, actual Agent/UI shared-draft updates, and feathered selection capture. A separately instrumented provider-fixture runtime also passed real placement with a hole and feathered mask, followed by receipt rollback. Its model response was synthetic. Photoshop 26.0.0 / UXP 8.0.1-release were independently read in this round; the original crash did not recur, but its cause remains unresolved. No active pixel selection remained after placement/rollback, so selection preservation is not established; actual non-1:1 scaling was not tested. No result transfers to the later #65 correction or other proposed heads.

| Package | Status | Evidence / remaining work |
|---|---|---|
| G00 | Implemented | 180 files imported with matching SHA256; separate CommonJS application; original 38 tests passed in the new directory; Alpha baseline tagged |
| G01 | Implemented | Shared validation, schemas, transitions, fixtures and module APIs; three domain boundary tests passed |
| G02 | Implemented and integrated | `bcf68a4`: immutable managed assets, revisioned drafts, durable jobs, deduplication and uncertain-outcome recovery; 24 module tests |
| G03 | Implemented; partial independent host validation | `8ab0e1` passed fresh capture/edit/rollback and document/history conflict cases. A separately labelled mock-provider runtime exercised real masked placement and rollback. The first-round crash remains unexplained; exact proposed-head acceptance and remaining native cases are pending |
| G04 | Implemented and integrated; live provider pending | `4cbb207`: Gemini BYOK, capability limits, bounded image parsing, cancellation and redacted errors; 16 fixture tests; no configured key or real generation |
| G05 | Integrated and independently reviewed | Professional workspace consumes shared drafts, source capture, jobs and results; native Chrome UI exercised the real UXP bridge; floating plugin panel visual/input acceptance remains pending |
| G06 | Implemented; focused HTTP correction independently retested | Foundation/#58: 17 Studio tools plus 7 existing Photoshop tools; private assembly: 33. On `8ab0e1`, real UXP registration worked with the shared guard, and an actual Agent updated the same draft that the professional UI displayed. Full Agent generation/placement and exact public-head acceptance remain separate |
| G07 | Implemented and reviewed | `fa789db`: 121 recipes, 101 numeric parameters, immutable source hashes, explicit reference mapping and two project Skills; 14 catalog/service tests |
| G08 | Not passed | [Round 1](evidence/2026-09-20-independent-review.md) and [round 2](evidence/2026-09-20-independent-review-round2.md) preserve different results and exact scopes. Round 2 adds native rollback, mock-provider masked placement and Agent/UI draft editing; native panel input, real-provider generation, COS samples and final proposed-head evidence remain missing |
| G09 / B05 | Implementation complete; host acceptance pending | #60: durable user library, read-only factory copy, edit/import/export/archive, optimistic history/restore, explicit reference mapping and same-source UI/MCP; browser assembly workflow exercised |
| G09 / B06 | Not started | Body silhouette / 17-part navigation remains separate; B05 does not complete G09 |
| G10–G21 | Not started | Later product phases remain in the approved scope |

Private assembly `6121333dd4fe9f99a065c2d7e76920bcf88b1992` passed **240 tests, 0 failures, 1 explicitly skipped launchd lifecycle test (241 total)**, checked 78 JavaScript files, built from a clean checkout and passed actual extracted-package startup/hash/UXP ZIP smoke checks. It has no aggregate PR. Native Chrome/CUA exercised an isolated browser runtime with a 320×240 synthetic source and two synthetic candidates: restoring model defaults, deriving a history-based draft, candidate B at 2× zoom, factory-copy/edit/load/archive/restore, key-free local model settings and 100–200% scaling. Read-only API checks corroborated the resulting state. No paid generation or Photoshop write occurred in these browser cases.

At their recorded feature heads, local full suites passed 209 tests / 1 skip for #59, 210 / 1 for #60, and 205 / 1 for #61, all with zero failures. The single skip in each is launchd lifecycle coverage. These suites overlap with each other and with the assembly; their counts must not be summed. Exact public feature heads are in the [delivery index](MODULAR-DELIVERY.md). CI records describe observed commits, not this new documentation commit or future retargeted heads.

The later #65 compatibility correction aligns native visibility targets with the SDK and restores only changed properties. Its exact public head passed 122/122 local tests, independent code review, hosted CI and downloaded-package verification; native acceptance remains pending. Private assembly `f0a622b162229fb99cc48cd5d45a55f5a82f17d2` combines it with the previous corrections and passed 261 tests / 0 failures / 1 launchd skip, 80 JavaScript checks, clean build and extracted startup. It has no aggregate PR and was not the subject of round 2's Photoshop operations.

Historical foundation regression at `f0cd2e5619a8819195377bc0a7a8cb32d85ae3af` passed **193/193**, including isolated launchd lifecycle coverage and 11 release-gate regressions; it checked 67 JavaScript files and passed extracted package checks. The first hosted run [passed on 469a608](https://github.com/lianshuang-photo/PXD/actions/runs/35464311154). These remain historical evidence and do not replace the newer feature checks or transfer host acceptance to another commit.

The production capability surface currently supports `image.edit` and `ps.layer.update` (name, opacity and visibility). Capture and placement are limited to 8 million pixels / 32 MiB, with RGB 8/16-bit capture adapted to sRGB8. PNG/JPEG return creates a new raster layer; grouping, WebP return and 32-bit HDR are explicitly unsupported. A large original needs a supported selection. Requesting 4K with automatic placement fails before a provider request; manual generation may retain the output, and actual unsupported output stays a managed asset without host dispatch.

CI/CD covers supported Node versions on Linux/macOS/Windows, reproducible runtime/source ZIPs, extracted startup, checksum manifests, SPDX inventory and provenance-backed draft releases. The release workflow requires protected main, final-head review and an immutable, complete real-host evidence record. It revalidates after owner environment approval and again before creating the tag. See [CI/CD](CI-CD.md). The release workflow itself has not been dispatched or claimed as a completed publication.

No main merge, installed Alpha service replacement or real model request has occurred in this implementation stage. Main remains `3c9fc7d25f0b9691346b5fdc0197a6e99305c676`; a fresh protection read confirmed administrator enforcement, both required checks, strict status checking, one approval, latest-push approval and stale-review dismissal. All 180 files in the preserved Downloads Alpha still match their manifest hashes; the annotated Alpha tag still dereferences to `535dd10629369ba4e36d67990a0f7b3cd4d2c0bc`. [Collaboration rules](COLLABORATION.md) define the merge gate. G00–G07 and B05 implementation do not mean G08, G09 as a whole, M0/M1/M2 or the complete COS-to-publication workflow has passed.
