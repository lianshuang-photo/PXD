# PR 验收记录模板

这是填写指南，不是已通过的报告。遵循仓库 [工程规范](../../../ENGINEERING-STANDARDS.md)。只填写实际执行过的内容；没有执行填“未验证”，不适用需说明与本次 diff 的关系。模板文字、打勾或截图都不是执行证据。

## 来源与范围

| 项目 | 实际值 |
|---|---|
| PR、比较 base、前置依赖 | |
| 被测 source commit（完整 SHA） | |
| 构建／生成插件目录与 source commit 的对应验证 | |
| 工作树是否含未提交改动（有则不能冒充该提交的最终验收） | |
| PS、UXP、OS、Companion 版本及插件标识 | |
| 使用的专用测试素材、服务和数据隔离方式 | |
| 实际操作面：浏览器／PS 后端／PS 插件面板 | |
| 执行者、日期、独立 reviewer | |

用户操作路径与预期结果：

受影响控件／公共契约／调用方／组装模块：

## 执行记录

| ID | 场景及前置状态 | 实际操作 | 观察到的 UI 与后端结果 | 状态及证据位置 |
|---|---|---|---|---|
| | 主成功路径 | | | 未验证 |
| | 失败与恢复路径 | | | 未验证 |
| | 共享组件／相邻模块回归 | | | 未验证 |

按本次改动增加适用行，明确哪些副作用真实发生，哪些使用 fixture。LS 功能场景另引用 [VALIDATION.md](../VALIDATION.md) 中适用的编号；本模板不替换 release 所要求的证据 schema。

## PS/UXP UI 变更的实际面板检查

本节适用于影响任一应用 PS/UXP 面板的改动，不能仅凭 React 或 HTML 技术栈判断宿主。填入实际面板尺寸。下表所有行从“未验证”开始，不能一次性声明“全通过”。浏览器专属 UI 在上面的执行记录中填写实际浏览器／尺寸／控件／状态检查，并说明原生面板不适用的原因；不涉及 UI 的改动同样注明原因。范围说明不自动满足 required status，也不改变既有 main／release 门禁。

| 主题 | 缩放 | 面板宽高 | 操作过的页面、控件和滚动路径 | 布局／可读性／输入结果与证据 |
|---|---|---|---|---|
| 浅色 | 100% | 常规： | | 未验证 |
| 浅色 | 100% | 窄： | | 未验证 |
| 浅色 | 200% | 常规： | | 未验证 |
| 浅色 | 200% | 窄： | | 未验证 |
| 深色 | 100% | 常规： | | 未验证 |
| 深色 | 100% | 窄： | | 未验证 |
| 深色 | 200% | 常规： | | 未验证 |
| 深色 | 200% | 窄： | | 未验证 |

| 控件／行为 | 实际操作与观察；状态／证据 |
|---|---|
| text、search、默认类型的 input、number、动态配方／预设字段 | 未验证 |
| 虚拟 password 遮罩、提交后清空及最终调用方错误无 Key | 未验证 |
| select 关闭／展开／选择／禁用；恢复默认后的实际参数 | 未验证 |
| textarea 长内容与焦点；物理 Enter／Shift+Enter／中文输入法确认 | 未验证 |
| 按钮鼠标／Enter／Space、disabled、重复激活；专用 checkbox/radio/file 行为 | 未验证 |
| 离线、加载、错误、成功、保存后刷新失败的实际提示 | 未验证 |

剪贴板粘贴不等于输入法确认，合成键盘事件不等于物理按键验证。共享控件修复需检查其他使用者，包括组装后才出现的字段。

## 结论和复查

- 自动化命令与结果：
- 浏览器交互结果（独立于 PS）：
- PS 后端执行结果（独立于面板视觉／输入）：
- PS 原生面板结果：
- 独立 reviewer、对应 commit、findings、修复与复查：
- 未解决问题／无法验证的原因、影响范围和下一步：
- 结论：未验证／不通过／通过（只能按实际适用范围填写）：

发生故障时保留具体失败和已完成步骤；不要覆盖旧报告。新的 source commit 需要新的检查结论。公开报告只含可共享测试素材与安全摘要，真实 Key、用户文档、会话、原图和完整运行日志保留在私有测试环境。`ls-studio/computer-use` 的成功设置仍服从 [CI/CD 证据契约](../CI-CD.md)，不得从本模板自动生成。
